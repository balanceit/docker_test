language: go

go:
  - 1.5
  - tip

services:
  - docker

sudo: false

install:
  - go get github.com/constabulary/gb/...
  - make build
  - make docker-image
  - make docker-run-daemon

script:
  - docker ps | grep -q go_web_server
  - make test
